program _PRG;
var int _AAA, _BBB, _X, _I;

{
    input(_AAA);
    input(_BBB);

    ?? квадрати чисел від A до B включно
    for _I := _AAA to _BBB {
        output(_I * _I);
    }

    ?? множення через додавання
    0 -> _X;
    for _I := 1 to _BBB {
        _X + _AAA -> _X;
    }

    output(_X);
}
end
